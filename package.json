{
  "name": "botdot",
  "version": "1.0.0",
  "description": "WhatsApp bot for laptop stock queries via WAHA + Odoo SQLite cache",
  "main": "src/server.js",
  "scripts": {
    "start": "node src/server.js",
    "sync": "node src/sync.js",
    "test": "node --test"
  },
  "engines": {
    "node": ">=22.5.0"
  },
  "dependencies": {
    "axios": "^1.7.2",
    "dotenv": "^16.4.5",
    "express": "^4.19.2",
    "node-cron": "^4.2.1",
    "xmlrpc": "^1.3.2"
  }
}
